const qunit = require("qunit");
const sinon = require("sinon");
const iohelpers = require("monio/io/all");
const { identity, inc, twice, eitherProp } = require("./utils");

qunit.module("io-all");

// qunit.test("placeholder", (assert) => {});
